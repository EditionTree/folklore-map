# Crawler policy for this site is set by Cloudflare's managed content signals,
# not here. Cloudflare prepends that block to this file at serve time, so the
# response a crawler actually receives carries:
#
#   User-agent: *
#   Content-Signal: search=yes, ai-train=no, use=reference
#   Allow: /
#
# plus Disallow rules for Amazonbot, Applebot-Extended, Bytespider, CCBot,
# ClaudeBot, CloudflareBrowserRenderingCrawler, Google-Extended, GPTBot and
# meta-externalagent.
#
# This file used to repeat those rules by hand. Because Cloudflare prepends
# rather than replaces, the served file ended up with TWO "User-agent: *"
# groups and six crawlers listed twice, which is malformed. The hand-written
# rules were dropped on 2026-09-01 so that one source owns the policy.
#
# Changing what is blocked now means changing the Cloudflare setting, not
# editing this file. Adding rules back here recreates the duplication.
#
# The site's full editorial and AI-use policy is at /editorial, and /llms.txt
# states the same terms in the form assistants read.

Sitemap: https://folklorefinder.uk/sitemap.xml
